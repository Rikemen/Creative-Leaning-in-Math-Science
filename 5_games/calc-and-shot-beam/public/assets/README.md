# 配信する素材

`images/`：背景とヒーロー、`audio/`：採用効果音、`icons/`：UIアイコン、`fonts/`：必要な配信フォント。

現在は初版の画像4枚、WAV効果音6種類、SVGアイコン4種類を配置済み。`manifest-v1.json` に配信パスを記録し、`../asset-preview.html` で確認できる。数字・HP・ビームは原則プログラムで描画する。画像生成の元データやプロンプトは `assets-source/` に置き、ここに混在させない。

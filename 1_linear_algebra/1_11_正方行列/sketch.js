
let a = new Matrix(2, 2);
let b = new Matrix(2, 3);
let c = new Matrix(3, 1);


function setup() {
    createCanvas(windowWidth, windowHeight);
    console.log("a is SquareMatix ? A. ", a.isSquareMatrix());
    console.log("b is SquareMatix ? A. ", b.isSquareMatrix());
    console.log("c is SquareMatix ? A. ", c.isSquareMatrix());



}

function draw() {
    background(176, 224, 230);

}

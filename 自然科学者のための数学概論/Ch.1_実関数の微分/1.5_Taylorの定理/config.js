/**
 * グラフ描画の設定
 * 数値定義や色のパレットを一括管理し、変更を容易にする。
 */
export const CONFIG = {
    grid: {
        cellSize: 50,  // グリッド1マスのピクセル数
        scale: 50,     // 数学的な「1」が何ピクセルに相当するか
    },
    style: {
        canvasWidth: 800,
        canvasHeight: 600,
        colors: {
            background: [250, 252, 255],
            grid: [220, 220, 220],
            axis: [100, 100, 100],
            text: [30, 30, 30],
            graph: [0, 120, 215],        // 元の関数: 信頼感のある青
            approx: [230, 40, 60],       // テイラー多項式: 警告色に近い赤（近似）
            remainder: [255, 120, 0],    // 剰余項（誤差）: オレンジ
            pointA: [0, 160, 80],        // 展開の中心 a: 緑
            pointX: [160, 32, 240],      // ターゲット点 x: 紫
            pointC: [255, 0, 150]        // ラグランシュ点 c: ピンク
        }
    }
};
